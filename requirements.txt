# This file lists the Python dependencies for the project.

# AMQP protocol client library (used by Celery)
amqp==5.3.1

# ASGI interface reference library – required for Django's asynchronous support
asgiref==3.9.1

# Multiprocessing support for Celery
billiard==4.2.1

# Celery task queue library
celery==5.5.3

# Command-line interface toolkit
click==8.2.1

# User-friendly command-line interface
click-didyoumean==0.3.1

# Click plugin system
click-plugins==1.1.1.2

# Click REPL support
click-repl==0.3.0

# Terminal text styling library
colorama==0.4.6

# Django web framework (latest 5.2.x version)
Django==5.2.4

# Django CORS headers support
django-cors-headers==4.7.0

# For managing environment variables in Django projects
django-environ==0.12.0

# Django REST framework for building Web APIs
djangorestframework==3.16.1

# Django REST framework JWT authentication support
kombu==5.5.4

# Library for parsing and handling version numbers (used by setuptools)
packaging==25.0

# Image processing support for Django (commonly used for handling media files)
pillow==11.3.0

# Library for building interactive command line applications (used by Click)
prompt_toolkit==3.0.52

# PostgreSQL database adapter for Python (used with Django's PostgreSQL backend)
psycopg2-binary==2.9.10

# Library for parsing, formatting, and manipulating dates and times
python-dateutil==2.9.0.post0

# Library for separating settings from code (used for managing environment variables)
python-decouple==3.8

# Python interface to the Redis key-value store (used with Celery)
redis==6.4.0

# setuptools is a package development and distribution library
setuptools==80.9.0

# Python 2 and 3 compatibility utilities
six==1.17.0

# SQL formatting and parsing library (used internally by Django)
sqlparse==0.5.3

# Timezone data for Python, useful when system tzdata is unavailable (esp. on Windows)
tzdata==2025.2

# Celery task queue library
vine==5.1.0

# Library for determining the number of terminal column cells a string occupies
wcwidth==0.2.13

# Width calculation for Unicode characters
wheel==0.45.1

